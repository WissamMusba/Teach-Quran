## Plan — Portrait polish + UX fixes + tutorial highlight fix

### 0) Tutorial highlight — remove the hack, measure correctly
- Remove global `NUDGE_Y = 24` in `src/tutorial/TutorialOverlay.tsx:65-73` (currently shifts every spotlight down 24px — wrong on tablets/other headers).
- Add `active={isCurrentPage}` to `reading-area` anchor in `src/screens/QuranViewScreen.tsx:2244` (currently registers from all 3 FlatList pages and clamps the wrong rect; same fix already correct for `reading-ribbon` in MushafPageView). Keep `tutorialRuntime.ts` guard + 400ms re-measure tick as-is.

### 1) Footer not covered by taskbar/nav bar (phones + tabs)
- Dashboard root respects `insets.bottom` (add `paddingBottom: insets.bottom` or wrap `DashboardFooter + CollapsibleBannerAd` with SafeArea bottom). Currently only footer pads; banner sits below with no inset and gets covered by gesture nav bar.
- Tabs: in `DashboardFooter.tsx` detect `width >= 600` and bump `minHeight 54→64`, `btn/pageCell minHeight 44→50`, `label 13→14` so "Go to page / Surah Index / Bookmarks" are visibly larger on tablets.

### 2) StudentHub header covered by notification/battery icons + back button clipped
- StudentHubScreen header uses fixed `paddingVertical:10`, no `useSafeAreaInsets`. Fix: `const insets = useSafeAreaInsets()` and `paddingTop: insets.top + 12`. Apply same to JuzIndex/SurahIndex if they share the pattern.

### 3) Light mode too bright — softer dashboard
- Current light: root `#F4F1EA`, cards `#FBF9F4`, footer white. Shift to warmer darker base: root `#EDE8DC`, cards/header/footer `#F5F0E6`, inputs `#EFECE4`. Dark mode untouched.

### 4) My Quran → back goes to Dashboard, not empty StudentHub
- Keep `handleMyQuranPress: Dashboard → QuranView {page}` (already correct). Add empty-hub guard in `StudentHubScreen`: `if (!currentStudent) navigation.replace('Dashboard')`, and make My Quran QuranView back do `popToTop / navigate('Dashboard')` when `isMyQuran`.

### 5) Register → auto-login
- In `RegisterScreen.tsx`, after `res.success` do `dispatch(setUser({id, username}))`, `dispatch(startTutorial())` if not done, `navigation.replace('Dashboard')` (same as LoginScreen). Same for login consistency.

### 6) Password eye icon → SVG
- Replace emoji 👁 + red slash View in `LoginScreen`/`RegisterScreen` with inline SVG `EyeOpen`/`EyeOff` (react-native-svg already imported).

### 7) Remove Indopak 2 everywhere (script + files)
- MCP shows no indexed Indopak 2 font, but check files: `indopak-15-lines-layout.sqlite`, `qudratullah-indopak-15-lines.db`, `app.json`/`settingsSlice` textStyle options, `quranData` script switch, `theme.ts` map. Remove any `indopak` option/asset and fallback.

### Execution order: 0 → 1+2 → 4+5 → 3 → 6 → 7 | Critical files: TutorialOverlay.tsx:44-77, QuranViewScreen.tsx:2244, StudentHubScreen:294-316, DashboardFooter:22-94, DashboardScreen:41-583, RegisterScreen:15-88, LoginScreen:20-91, theme.ts:20-33.